---
layout: single
title: "스프링MVC 기본 기능"
categories: [mvc1]
tags: [mvc1, TIL]
toc: true
author_profile: true
---

# ☕ Spring 공부 기록

## 📘 학습 날짜
- 2025-07

## 📅 오늘 배운 내용

### 1. 스프링 MVC 시작

- 애노테이션 기반의 컨트롤러 
- @Controller - 스프링이 자동으로 스프링 빈으로 등록(내부에 @Component 있음), 반환 값이 String이면 뷰 이름으로 인식
- @RequestMapping - 해당 URL이 호출되면 이 메서드 호출
- @RequestParam("...") - HTTP 요청 파라미터를 받는 애노테이션(request.getParameter(...)와 거의 같은 코드)
- @RestController - HTTP 메시지 바디에 바로 반환 값 입력.
- @PathVariable - 매칭 되는 부분 편리하게 조회 가능, 이름과 파라미터 이름이 같으면 생략 가능(/mapping/{userId} -> @PathVariable String userId)
- HTTP 요청 헤더 정보 조회도 가능하다.


### 2. HTTP 요청 데이터 조회

**3가지 방법 존재**

- GET 쿼리 파라미터(/mapping?username=hello&age=10, 검색, 필터, 페이징 등에서 많이 사용)
- POST - HTML Form(message body에 쿼리 파라미터 형식으로 전달 username=hello&age=10, 회원가입, 상품 주문, HTML Form사용 시)
- HTTP message body에 직접 담아서 요청(HTTP API에서 주로 사용, JSON 주로 사용)


---
- @RequestParam(required = true) String username시, /request?username= -> 빈 문자로 통과 , /request -> username 없으므로 400예외 발생
- @RequestParam(required = false) int age -> 기본형 int에 null입력하는 것은 불가능, Integer로 변경 혹은 defaultValue 옵션 사용
- @ModelAttribute HelloData helloData -> helloData클래스에 @Setter,@Getter 존재한다면 요청 파라미터 이름으로 파라미터 값을 바인딩해줌.
- @ResponseBody - 응답 결과 HTTP 메시지 바디에 직접 담아서 전달 가능.
- @RequestBody - HTTP 메시지 바디 정보 편리하게 조회 가능.
- 헤더 정보 필요하면 @RequestHeader 사용하면 됨.

**요청파라미터 조회는 @RequestParam,@ModelAttribute, HTTP 메시지 바디 직접 조회는 @RequestBody**
