---
layout: single
title: "Queue,Stack,Deque 정리"
categories: [java]
tags: [java]
toc: true
author_profile: true
---

# 자바에서 Queue / Stack / Deque 정리

## 1. 인터페이스와 구현체
- **인터페이스**: 동작 규격만 정의 (`List`, `Queue`, `Deque`)
- **구현체**: 인터페이스를 실제로 구현한 클래스
  - `ArrayList` → List 전용 구현체 (배열 기반)
  - `LinkedList` → List + Deque 구현체 (이중 연결 리스트 기반)
  - `ArrayDeque` → Deque 구현체 (배열 기반, 성능 우수)

---

## 2. 주요 개념
### Queue (큐)
- **FIFO (First-In-First-Out)**: 먼저 들어온 원소가 먼저 나감
- 대표 메서드:  
  - `offer(x)` / `offerLast(x)` : 뒤에 삽입  
  - `poll()` / `pollFirst()` : 앞에서 꺼냄  
- 예: 은행 대기줄

### Stack (스택)
- **LIFO (Last-In-First-Out)**: 나중에 들어온 원소가 먼저 나감
- 대표 메서드:  
  - `push(x)` / `offerFirst(x)` : 앞에 삽입  
  - `pop()` / `pollFirst()` : 앞에서 꺼냄  
- 예: 접시 쌓기

### Deque (덱, Double-Ended Queue)
- 양쪽 끝에서 삽입/삭제 모두 가능
- 조합에 따라 Queue 또는 Stack처럼 동작

| 삽입 | 꺼내기 | 동작 | 예시 (1,2,3 삽입) | 결과 |
|------|--------|------|--------------------|------|
| offerLast | pollFirst | **큐(FIFO)** | [1,2,3] | 1,2,3 |
| offerFirst | pollLast | **큐(FIFO)** | [3,2,1] | 1,2,3 |
| offerLast | pollLast | **스택(LIFO)** | [1,2,3] | 3,2,1 |
| offerFirst | pollFirst | **스택(LIFO)** | [3,2,1] | 3,2,1 |

---

## 3. 시간복잡도 정리
| 연산 | ArrayList | LinkedList | ArrayDeque |
|------|-----------|------------|------------|
| `addLast` / `offerLast` | **O(1)** (가끔 resize 시 O(n)) | O(1) | O(1) (원형 배열) |
| `addFirst` / `offerFirst` | O(n) (앞으로 shift 필요) | O(1) | O(1) |
| `removeFirst` / `pollFirst` | O(n) (앞에서 shift 필요) | O(1) | O(1) |
| `removeLast` / `pollLast` | O(1) | O(1) | O(1) |
| `get(index)` | **O(1)** | O(n) | ❌ (index 접근 없음) |
| 메모리 오버헤드 | 낮음 | 높음 (노드 객체 2포인터) | 낮음 |

👉 정리:
- **ArrayList** → 인덱스 접근 최강, 하지만 큐/덱 연산에는 부적합  
- **LinkedList** → 앞/뒤 삽입삭제 O(1), 하지만 캐시 비효율 + 인덱스 접근 느림  
- **ArrayDeque** → 앞/뒤 삽입삭제 모두 O(1), 빠르고 메모리 효율적 → **실무에서 큐/스택 구현체로 가장 권장**

---

## 4. 실무에서의 사용 가이드
- **구현체 선택**: 거의 항상 `ArrayDeque` 권장 (빠르고 가볍고 안정적)
- **타입 선언**: 의도를 드러내기 위해 인터페이스로 선언
  - 큐만 쓸 경우 → `Queue<Integer> q = new ArrayDeque<>();`
  - 스택만 쓸 경우 → `Deque<Integer> stack = new ArrayDeque<>();`

### 왜 이렇게?
- `Deque`는 Queue와 Stack을 모두 흉내낼 수 있음
- 하지만 **코드 가독성**을 위해 필요한 기능만 드러나는 인터페이스를 사용
- 다른 개발자가 코드를 읽을 때 의도를 쉽게 파악할 수 있음

---

## 5. 요약
- `Queue` = FIFO → `offerLast` + `pollFirst`
- `Stack` = LIFO → `push` + `pop` (또는 `offerFirst` + `pollFirst`)
- `Deque` = 큐/스택 둘 다 구현 가능
- **실무에서는 구현체는 `ArrayDeque`, 타입은 상황에 맞게 `Queue` or `Deque` 로 선언**
