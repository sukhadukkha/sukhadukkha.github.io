---
layout: single
title:  "AWS SAA 준비(Organization,Resource-based Policies vs IAM Roles, IAM Policy Evaluation Logic, IAM Identity Center,Control Tower)"
categories: [AWS SAA]
tags: [AWS SAA]
toc: true
author_profile: true
---


## AWS Organizations

---

* **AWS Organizations란?**

    * 여러 AWS 계정을 **중앙에서 통합 관리**하기 위한 서비스
    * 계정 생성, 정책 적용, 비용 관리, 보안 통제를 일관되게 수행

---

* **왜 필요한가?**

    * 계정이 늘어날수록

        * 보안 정책 관리 어려움
        * 비용 추적 복잡
    * Organizations로 중앙 통제

---

* **핵심 개념**

    * Management Account

        * 조직을 생성하고 관리하는 최상위 계정

    * Member Account

        * 조직에 포함된 개별 AWS 계정

    * Organizational Unit (OU)

        * 계정을 논리적으로 그룹화
        * OU 단위로 정책 적용 가능

---

* **Service Control Policy (SCP)**

    * 개념

        * 계정 또는 OU에서 **사용 가능한 AWS 서비스/액션의 최대 범위**를 제한

    * 특징

        * 허용 정책(Allow List) 기반
        * IAM 권한보다 우선 적용
        * 실제 권한을 부여하지는 않음

    * 예시

        * 특정 OU에서 S3 삭제 금지
        * 특정 계정에서 리전 사용 제한

---

* **SCP vs IAM Policy**

    * SCP

        * 조직 전체 관점의 상한선
        * "이 계정은 여기까지 가능"

    * IAM Policy

        * 사용자/역할 단위 권한 부여
        * "이 사용자는 무엇을 할 수 있는가"

---

* **비용 관리**

    * Consolidated Billing

        * 여러 계정의 비용을 하나로 통합
        * 사용량 합산으로 할인 효과

    * 비용 분리

        * 계정별 비용 추적 가능

---

* **보안 활용**

    * 조직 단위 보안 정책 강제
    * 루트 계정 사용 제한
    * 특정 서비스 사용 금지

---

* **시험 자주 나오는 시나리오**

    * "모든 계정에서 특정 서비스 사용 금지"

        * → SCP

    * "계정은 분리하되 비용은 통합 관리"

        * → AWS Organizations

    * "부서별 계정 관리"

        * → OU 활용

---

* **시험 핵심 포인트 요약**

    * AWS Organizations = 다계정 관리
    * SCP = 권한 상한선
    * 실제 권한 부여 ❌
    * 보안 + 비용 관리 목적


---


## Resource-based Policy vs IAM Role

---

## 한 문장으로 먼저 이해하기

* **Resource-based Policy**

    * 👉 "**이 리소스에 누가 들어올 수 있는지**"를 리소스가 직접 정함

* **IAM Role**

    * 👉 "**이 사람이(서비스가) 어떤 권한을 빌려서 일할 수 있는지**"를 정함

---

## Resource-based Policy 쉽게 이해하기

* **개념**

    * 정책을 **리소스에 직접 붙임**
    * 리소스가 "너는 들어와도 돼"라고 말하는 방식

* **핵심 포인트**

    * 정책 안에 **누가(Principal)** 접근하는지 명시
    * 다른 AWS 계정도 바로 접근 가능

---

### 예시 1️⃣ (S3 버킷 공유)

* 상황

    * A 계정에 S3 버킷이 있음
    * B 계정이 이 버킷에 파일을 업로드해야 함

* 해결 방법

    * S3 버킷에 **Bucket Policy** 설정
    * Principal에 B 계정 지정

* 왜 Resource-based Policy?

    * B 계정이 **버킷에 직접 접근**해야 함
    * 역할을 빌릴 필요 없음

👉 시험 정답: **Resource-based Policy**

---

### 예시 2️⃣ (SQS / SNS 접근)

* 상황

    * 다른 계정의 애플리케이션이 내 SQS 큐에 메시지 전송

* 해결 방법

    * SQS Queue Policy에 상대 계정 허용

👉 시험 정답: **Resource-based Policy**

---

## IAM Role 쉽게 이해하기

* **개념**

    * 권한 묶음(역할)을 만들어서 **누군가에게 맡김**
    * 필요할 때만 잠깐 권한을 빌려 씀

* **핵심 포인트**

    * **AssumeRole** 필요
    * 임시 자격 증명 사용

---

### 예시 3️⃣ (EC2가 S3 접근)

* 상황

    * EC2에서 S3 파일을 읽어야 함
    * Access Key를 코드에 넣고 싶지 않음

* 해결 방법

    * EC2에 IAM Role 부여
    * Role에 S3 접근 권한 정책 연결

👉 시험 정답: **IAM Role**

---

### 예시 4️⃣ (Cross-account 관리자 작업)

* 상황

    * A 계정 관리자가 B 계정의 리소스를 관리해야 함

* 해결 방법

    * B 계정에 Admin Role 생성
    * A 계정 사용자가 해당 Role을 Assume

👉 시험 정답: **IAM Role**

---

## 헷갈릴 때 쓰는 판단 공식

* "**리소스에 바로 접근한다**" → Resource-based Policy
* "**권한을 빌려서 작업한다**" → IAM Role

---

## 시험용 초간단 요약

* Resource-based Policy

    * 리소스 중심
    * 바로 접근

* IAM Role

    * 주체 중심
    * AssumeRole + 임시 권한

---

## 한 줄 암기

* Resource-based Policy → "**들어와도 되는 사람 명단**"
* IAM Role → "**이 역할을 맡아라**"

---


## IAM Policy Evaluation Logic (권한 평가 로직)

---

## 한 줄 개념

* **IAM Policy Evaluation Logic**란?

    * "이 요청을 **허용할지 / 거부할지** AWS가 판단하는 순서와 규칙"

---

## 기본 원칙 (시험 최중요)

1️⃣ **기본값은 Deny (거부)**
2️⃣ **Explicit Deny는 항상 최우선**
3️⃣ **Allow가 있어야만 허용**

---

## 전체 평가 흐름 (순서 중요)

### Step 1️⃣ 요청 발생

* 사용자 / 역할 / 서비스가 AWS API 요청

---

### Step 2️⃣ 적용 가능한 모든 정책 수집

* IAM Identity-based Policy

    * 사용자, 그룹, 역할에 연결된 정책

* Resource-based Policy

    * S3 Bucket Policy, SQS Policy 등

* SCP (AWS Organizations)

    * 계정/OU의 최대 허용 범위

* Permission Boundary (있다면)

---

### Step 3️⃣ Explicit Deny 확인

* 아래 중 **하나라도 Deny** 있으면

    * ❌ 즉시 거부

* Deny 출처

    * IAM Policy
    * Resource-based Policy
    * SCP
    * Permission Boundary

---

### Step 4️⃣ Allow 여부 확인

* Deny가 없을 때

* Allow가 **하나라도 존재**하면

    * ⭕ 허용

* Allow가 전혀 없으면

    * ❌ 암묵적 거부 (Implicit Deny)

---

## 그림으로 이해하기 (머릿속 이미지)

```
요청
 ↓
모든 정책 수집
 ↓
Explicit Deny 있음?
 ├─ Yes → 거부 ❌ (끝)
 └─ No
      ↓
Allow 있음?
 ├─ Yes → 허용 ⭕
 └─ No → 거부 ❌ (Implicit Deny)
```

---

## 쉬운 예시 1️⃣ (Allow vs Deny)

* IAM Policy

    * S3:GetObject → Allow

* SCP

    * S3:* → Deny

👉 결과: ❌ 거부

* 이유: **SCP의 Explicit Deny가 최우선**

---

## 쉬운 예시 2️⃣ (Allow 없음)

* 어떤 정책에도 EC2:StartInstances Allow 없음

👉 결과: ❌ 거부

* 이유: **Implicit Deny**

---

## 쉬운 예시 3️⃣ (Resource-based Policy)

* S3 Bucket Policy

    * 특정 계정 Allow

* 요청 주체

    * 해당 계정 사용자

👉 결과: ⭕ 허용

* 이유: **Resource-based Policy의 Allow도 유효**

---

## 시험에서 자주 나오는 함정 포인트

* SCP는

    * ❌ 권한 부여 안 함
    * ⭕ Allow 상한선 역할

* Resource-based Policy의 Allow는

    * Identity Policy Allow 없어도 접근 가능

* Explicit Deny는

    * 어디에 있든 무조건 우선

---

## 시험용 초압축 요약

* 기본 = Deny
* Explicit Deny > Allow
* Allow 없으면 Implicit Deny
* SCP는 상한선
* Resource-based Allow 유효

--- 

## IAM Identity Center (구 AWS SSO)

---

## 한 줄 개념

* **IAM Identity Center**란?

    * 여러 AWS 계정과 애플리케이션에 대해 **하나의 로그인으로 접근(Single Sign-On)** 하게 해주는 서비스

---

## 왜 필요한가?

* 기존 방식 문제

    * 계정마다 IAM User 생성
    * 비밀번호 관리 복잡
    * 퇴사/권한 변경 시 관리 어려움

* Identity Center 도입

    * 중앙에서 사용자 관리
    * 한 번 로그인 → 여러 계정/서비스 접근

---

## 핵심 구성 요소

* **User / Group**

    * Identity Center에서 직접 생성하거나
    * 외부 IdP와 연동

* **Permission Set**

    * AWS 계정에 부여할 권한 템플릿
    * 실제로는 IAM Role로 생성됨

* **Account Assignment**

    * 어떤 사용자/그룹이
    * 어떤 AWS 계정에서
    * 어떤 Permission Set을 사용할지 정의

---

## 동작 흐름 (시험 핵심)

1️⃣ 사용자가 Identity Center 포털 로그인
2️⃣ 접근 가능한 AWS 계정 목록 확인
3️⃣ 계정 선택
4️⃣ Permission Set 기반 **IAM Role 자동 Assume**
5️⃣ 콘솔 / CLI 접근

---

## Permission Set 이해하기

* 개념

    * IAM Policy 모음 템플릿

* 특징

    * 여러 계정에 재사용 가능
    * 관리 일관성 확보

* 예시

    * ReadOnlyAccess Permission Set
    * Developer Permission Set
    * Admin Permission Set

---

## 외부 IdP 연동

* 지원 방식

    * SAML 2.0
    * OIDC

* 대표 연동 대상

    * Azure AD
    * Okta
    * Google Workspace

---

## IAM User vs IAM Identity Center

* IAM User

    * 계정 단위
    * 장기 자격 증명

* IAM Identity Center

    * 조직 단위
    * SSO 기반
    * 임시 자격 증명 사용

---

## 시험 자주 나오는 시나리오

* "여러 AWS 계정에 하나의 로그인으로 접근"

    * → IAM Identity Center

* "회사 직원 계정 중앙 관리"

    * → IAM Identity Center + Organizations

* "외부 IdP 연동"

    * → IAM Identity Center

---

## 시험 핵심 요약

* IAM Identity Center = AWS SSO
* 여러 AWS 계정 + 중앙 사용자 관리
* Permission Set → IAM Role 자동 생성
* 임시 자격 증명 사용

---

## 실무 로그인 예시 (AD / Google / Okta 연동)

### 구성 요약

* 회사 IdP: **Google Workspace (또는 AD / Okta)**
* AWS: **IAM Identity Center + Organizations**
* AWS 계정: **Dev / Prod**

---

### 사용자/그룹

* 사용자

    * 철수 (개발자)
    * 영희 (운영자)

* 그룹

    * Developers
    * Operators

---

### Permission Set

* Developer Permission Set

    * 개발 리소스 생성/수정

* Operator Permission Set

    * 배포/운영 권한

* ReadOnly Permission Set

    * 조회 전용

---

### Account Assignment (핵심)

* **Dev 계정**

    * Developers → Developer Permission Set
    * Operators → Operator Permission Set

* **Prod 계정**

    * Developers → ❌ 접근 불가
    * Operators → Operator Permission Set

---

### 실제 로그인 흐름

1️⃣ 직원이 **회사 계정(Google/AD/Okta)** 으로 로그인
2️⃣ IAM Identity Center로 SSO
3️⃣ 접근 가능한 AWS 계정 목록 표시
4️⃣ 계정 선택
5️⃣ Permission Set 기반 **IAM Role 자동 Assume**
6️⃣ AWS 콘솔/CLI 접근

---

### 퇴사/권한 변경 시

* 회사 IdP에서 계정 비활성화
* → AWS 접근 즉시 차단
* → IAM User 정리 불필요

---

## 시험 한 줄 암기

* **사람 로그인** → 회사 IdP
* **AWS 권한** → Identity Center + Permission Set
* **직원에게 IAM User 생성 ❌**


---


## AWS Control Tower

---

## 한 줄 개념

* **AWS Control Tower**란?

  * AWS Organizations 기반으로 **멀티 계정 환경을 자동으로 구성·관리**해주는 서비스
  * "**모범 사례가 적용된 다계정 AWS 환경을 한 번에 만들어주는 서비스**"

---

## 왜 필요한가?

* 계정이 많아질수록

  * 보안 설정 제각각
  * 로그 수집 누락
  * 계정 생성 실수

👉 Control Tower로 **표준화 + 자동화**

---

## Control Tower가 해주는 일 (핵심)

1️⃣ AWS Organizations 자동 생성
2️⃣ 기본 계정 구조 생성
3️⃣ 보안/감사 가드레일 적용
4️⃣ 계정 생성 자동화 (Account Factory)

---

## 기본 계정 구조 (시험 중요)

```
Root (Management Account)
 ├─ Log Archive Account
 ├─ Audit Account
 └─ Other Member Accounts (Dev / Prod 등)
```

* **Log Archive Account**

  * CloudTrail / Config 로그 중앙 저장

* **Audit Account**

  * 보안팀 / 감사팀 전용

---

## Guardrails (시험 단골)

* **Guardrail이란?**

  * 계정 전체에 적용되는 **규칙 세트**

### 유형

* **Preventive Guardrail**

  * SCP 기반
  * 아예 실행 불가

* **Detective Guardrail**

  * AWS Config 기반
  * 위반 감지 및 알림

---

## Account Factory

* 개념

  * 표준 설정이 적용된 AWS 계정을 **자동으로 생성**

* 포함되는 것

  * 네트워크 기본 구성
  * 로그 설정
  * Guardrail 적용

---

## Control Tower vs Organizations

* AWS Organizations

  * 계정 묶기 + SCP

* Control Tower

  * Organizations + **자동화 + 베스트 프랙티스**

👉 Control Tower ⊃ Organizations

---

## IAM Identity Center와의 관계

* Control Tower는 기본적으로

  * **IAM Identity Center 활성화**
  * 계정 접근 관리 연계

---

## 실무 예시

* 회사가 AWS 처음 도입
* 여러 팀이 각자 계정 필요

👉 Control Tower로

* 보안 표준 유지
* 계정 난립 방지

---

## 시험 자주 나오는 시나리오

* "Set up a multi-account AWS environment following best practices"

  * → AWS Control Tower

* "Automatically enforce guardrails across accounts"

  * → AWS Control Tower

---

## 시험 핵심 요약

* Control Tower = 다계정 자동화
* Guardrail = 계정 규칙
* Preventive = SCP
* Detective = Config
* Organizations 기반
