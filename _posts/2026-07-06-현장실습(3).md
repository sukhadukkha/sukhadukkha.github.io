---
layout: single
title:  "0706 현장실습"
categories: [현장실습]
tags: [Linux]
toc: true
author_profile: true
---

# 07/04

1. 리눅스 나눠지는 것들 종류들
2. 레드햇 리눅스는 어떤거고 왜 사용할까 (사용률 80% 정도)
3. 페도라 프로젝트 -> 레드햇이 운영(뿌리 페도라)
4. EPEL(Extra Packages for Enterprise Linux) -> 페도라 안에 있는 레드햇에 없는 패키지들 다 있음
5. CentOS (원래 레드햇에서 무료로 풀고 지원해주다가 몇년전에 취소함) -> 다 마이그레이션 (redhat으로), redhat 리눅스 마지막 테스트버전 느낌
6. 오픈시프트 -> 레드햇 코어OS위에 올라감
7. 고객사 -> 쉘 커스터마이징 해서 사용 , 쉘 종류 많음, 선택해서 사용 
각자 장단점 존재
8. GNOM X, 텍스트모드 권장 (실제 운영시스템에서는)
9. 디렉터리 구조, 각각 파일 무슨역할인지
10. /bin : 일반 사용자 권한으로 실행할 수 있는 바이너리 파일


/boot : 운영체제 부팅 시 필요, 부트로더, 이미지<br>
/dev (device) : 하드웨어 장치 파일들<br>
/etc : 각종 Service 설정 파일들, 각종 소프트웨어들<br> 설정파일
/home : 각각의 사용자 디렉터리<br>
/ : 최상위 디렉터리<br>
/root : 관리자 디렉터리<br>
/run : 서비스 실행중일 때 임시로 생성되는 파일들, 실시간 업데이트 정보값들<br>
특정 PID 정보 확인 가능<br> 
/sbin : root만이 사용할 수 있는 binary 디렉터리<br>
/tmp : sticky 비트 적용되어져서 만든 사용자만 실행 가능, 루트는 예외<br>
/usr : 리눅스 용 소프트웨어들 대부분 /usr 밑에 설치됨<br>
/var : 실시간 데이터 동적 변경, 가벼운 로그 같은것들<br>

11. /bin => /usr/bin
/sbin => /usr/sbin
/lib => /usr/lib
/lib64 => /usr/lib64 이렇게 링크를 만들어둔 이유 : 보안에 취약하기에 최상위 디렉터리로 누군가가 접근 막기, 소프트웨어들이 /usr 디렉터리 기본으로 하기에, 그 밑에 있는 디렉터리 참조하기 위해


---------
# 07/06

1. 절대경로 상대경로 말로 할 수 있을정도 -> 절대경로는 파일의 경로를 다 적는 방식이고, 상대경로는 현재 내 위치를 기준으로 나타내는 경로이다.
2. rm -rfi 중 f가 우선임
3. rm으로 한번에 지울때는 rm -rf (조심해야함)
4. 링크는 보통 심볼릭링크 사용 ln -s
5. 하드링크는 원본파일(A)가 지워져도 링크파일 B는 살아남음, 디렉터리는 안되고 파일만 가능
6. 소프트링크는 B는 쓸모없어짐
7. inode -> 특정 파일의 정보를 가지고 있음. (상태, 타입, 위치, 그룹, 소유권, 권한 등등) , 파일 식별
8. 파일시스템 포맷 별로 -> 파일시스템 크기에 의해서 inode 크기 제한 (inode 꽉찰수있음)
9. 하드링크는 동일한 파일시스템일때만 작동함
10. 심볼릭링크는 파일시스템 달라도 연결 가능
11. 특정 파일 지울때는 심볼릭링크도 같이 지워야함 ( 나중에 그 심볼릭 링크 다시 연결될수있음 , 동일한 파일명 생성시)

ln -s [원본(목적지)] [링크이름]

링크이름이 이미 디렉터리이면
그 디렉터리 안에 링크 생성

12. *, ? , [abc...] , [!abc...] 는 가끔 사용, 눈에 익혀놓자
13. 대괄호 중괄호 소괄호 구분(ShellScript 작성 시 사용하면 편함)
14. 항상 변수표현시에 중괄호 써줘라
15. a=date , a=$(date), a=`date`(백틱) '' 와 헷갈릴 수 있으니 가능하면 $사용
16. a=$(date) echo '$a' , echo "$a", echo $a 의 차이 \$a 는 문자그대로 표시

17. 파일시스템은 저장장치(HDD, SDD 등)에 파일을 저장하고 관리하는 규칙이다.

파일 이름, 파일 위치, 파일 크기, 디렉터리 구조, 권한 등의 정보를 관리한다.

SSD나 HDD는 단순히 데이터를 저장하는 공간일 뿐 파일을 직접 관리하지 못한다. 따라서 파일의 위치와 정보를 체계적으로 관리하기 위한 규칙이 필요한데, 이를 파일시스템이라고 한다.

18. ext4 : 안정성, 성능 좋은 기본 파일시스템
19. xfs : RHEL 계열 기본, 대용량 파일 처리 우수, 서버 환경에서 많이 사용
20. ''는 문자 그대로, ""는 변수값 출력
21. /dev/null 의 역할 (출력 값 버리는 곳)
22. 파이프라인 -> 이전의 명령에서 출력되는 값을 파이프라인 이후에 사용하는 옵션의 인자값으로 넘겨줌
23. 리다이렉션 & 의미
24. $PATH가 뭔지 -> $PATH는 리눅스 터미널에 명령어를 입력했을 때, 시스템이 그 명령어 프로그램(실행 파일)을 어디서 찾을지 미리 지정해 둔 '디렉터리 경로들의 목록'이다. 만약 없었다면 /usr/bin/ls 이런식으로 명령어를 쳤어야 했을 것
25. A라는 계정에 접속하면 가장먼저 /etc/profile, /etc/bashrc 적용 -> 그 다음 그 유저의 home 밑에 있는 ./bashrc, ./bash_profile 적용되고 로그인 됨.
26. "로그인 쉘은 시스템 전체 환경 설정을 위해 /etc/profile과 유저의 ~/.bash_profile을 시작으로 모든 파일을 다 읽어 들이지만, 비로그인 쉘은 로그인 과정을 생략하므로 profile 파일들은 건너뛰고 ~/.bashrc와 /etc/bashrc 같은 쉘 설정 파일만 실행한다."
27. 로그인 쉘, 비로그인 쉘 로그인 방식에 의해 실행되는 파일 다르다.

5장

C
A
D
C
B
C
D

```
[jiho@vm1 ~]$ export EDITOR=vim
[jiho@vm1 ~]$ 
[jiho@vm1 ~]$ echo $EDITOR
vim

이렇게 하면 출력되고, 비로그인 쉘로 접속시 [jiho@vm1 ~]$ echo $EDITOR

이렇게 떠 이건 왜그런걸까? 

그리고 고객들 입장에서 어떤 유저는 어떤 변수 추가, 어떤 유저든 항상 어떤 변수 추가 하려고 요청들어오면 어떻게 해야하는지

-> export EDITOR=vim 이 변수는 현재 켜져있는 터미널 창의 메모리에만 임시 저장된다.
새로운 창(비로그인 쉘)을 열거나, 다른 세션으로 접속하면 리눅스는 완전히 새로운 메모리 공간(프로세스)를 만든다.

영구히 저장하려면? -> /etc/profile 등)에 코드로 적어두어야한다.

유형 1. 어떤 유저(특정 유저)에게만 변수를 추가해주세요.

요청 예시: "개발자 jiho 계정에서만 톰캣 경로(TOMCAT_HOME)를 쓸 수 있게 해주세요."
작업 위치: jiho 유저의 홈 디렉터리에 있는 ~/.bash_profile을 수정한다.
이유: 다른 사용자들에게는 영향을 주지 않고, 오직 그 유저가 로그인할 때만 방을 독립적으로 세팅해준다.

vi /home/jiho/.bash_profile -> export TOMCAT_HOME=/usr/local/tomcat
설정 바로 적용 -> source /home/jiho/.bash_profile

유형 2. 어떤 유저든 항상 변수가 추가되게 해주세요.

요청 예시: "서버 보안 정책상 모든 사용자의 기본 에디터를 vim으로 고정하고, 타임아웃 설정을 넣어주세요."
작업 위치: /etc/profile or /etc/profile.d 밑에 스크립트를 작성한다.
이유: 리눅스 시스템에 존재하는 root를 포함한 모든 계정이 로그인할 때 무조건 첫 번째로 읽는 공용 파일이기 때문이다.

vi /etc/profile
export EDITOR=vim
export TMOUT=600
source /etc/profile

3. /etc/profile.d/ 활용하기

금융권 인프라에서는 /etc/profile 파일을 직접 수정하는 것을 꺼린다. 실수로 파일 내부 코드를 건드리면 시스템 전체 유저의 로그인이 꼬이기 대문이다.
그래서 /etc/profile.d/ 디렉터리를 제공한다.

vi /etc/profile.d/custom_vars.sh
export EDITOR=vim
export MY_SERVER_ROLE=PROD

이렇게 해두면 리눅스가 로그인할 때, /etc/profile.d/ 안에 있는 모든 .sh 파일을 자동으로 읽어들인다.
```

- ~/.bash_profile : 오직 로그인 할때만 한 번 동작
- ~/.bashrc : 비로그인 쉘을 열 때마다 매번 동작
- 특정 유저한테만 alias 적용할 때, `/home/[유저]/.bashrc` 수정 -> vi /home/jiho/.bashrc -> alias ll='ls -al'
- 전체 사용자에게 alias 적용할 때, `/etc/bashrc` -> vi /etc/bashrc -> alias rm='rm -i'
- 전체 사용자에 대한 alias도 보통 /etc/profile.d 에서 작업한다.

## 07/06 실습

- 스크립트 작성 시 #!/usr/bin/bash와 #!/bin/bash와 뭐가 다른걸까?
    - bash 실행파일의 위치 차이 but /bin -> usr/bin로 소프트링크 되어있어 두개는 동일하다.

```
mkdir -p /Documents/glob; cd glob
touch  touch alfa bravo charlie delta echo able baker cast dog easy
ls *a* , ls br* , ls [ac]*, ls ???, 

[root@vm1 glob]# echo {Sunday,Monday,Tuesday,Wednesday}.log
Sunday.log Monday.log Tuesday.log Wednesday.log
[root@vm1 glob]# echo file{1..3}.txt
file1.txt file2.txt file3.txt
[root@vm1 glob]# echo file{1...3}.txt
file{1...3}.txt
[root@vm1 glob]# echo file{a,b}{1,2}.txt
filea1.txt filea2.txt fileb1.txt fileb2.txt
[root@vm1 glob]# 
[root@vm1 glob]# 
[root@vm1 glob]# echo file{a{1,2},b,c}.txt
filea1.txt filea2.txt fileb.txt filec.txt

mkdir -p ../RHEL{7,8,9}
[root@vm1 glob]# ls ..
anaconda-ks.cfg  Documents  glob  initial-setup-ks.cfg  RHEL7  RHEL8  RHEL9
[root@vm1 glob]# ls ../RHEL*
../RHEL7:

../RHEL8:

../RHEL9:


[root@vm1 glob]# USERNAME=a
[root@vm1 glob]# echo ${USERNAME}
a

[root@vm1 glob]# echo The value of $HOME is your home directory.
The value of /root is your home directory.
[root@vm1 glob]# echo The value of \$HOME is your home directory
The value of $HOME is your home directory

[root@vm1 glob]# echo "***** hostname is ${myhost} *****"
***** hostname is vm1 *****
[root@vm1 glob]# echo '***** hostname is ${myhost} *****'
***** hostname is ${myhost} *****

[root@vm1 glob]# echo "Will variable $myhost evaluate to $(hostname -s)?"
Will variable vm1 evaluate to vm1?
[root@vm1 glob]# echo 'Will variable $myhost evaluate to $(hostname -s)?'
Will variable $myhost evaluate to $(hostname -s)?

echo "\"Hello World!"\" "$(date)" == echo '"Hello World!" '"$(date)"
```


```
#!/bin/bash

echo "Hello World!"
echo "ERROR..." >&2

hello > output.txt 

[jiho@vm1 bin]$ hello > output.txt
ERROR: Houston, we have a problem.

[jiho@vm1 bin]$ hello 2> output.txt
Hello, world
```

- "ERROR..." > &2 : 지금 내보내는 출력을 2번 통로로 보내라
- hello 2> error.log : 표준 출력은 출력하고, 표준 에러 출력을 error.log에 저장해라
- /bin/backup_script.sh > backup.log 2> error.log
- /bin/backup_script.sh > total.log 2>&1
  - 2번 표준 에러 출력을 1번 통로로 보내라. (출력, 에러 다 합쳐서 저장하기)

